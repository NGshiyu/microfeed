interface:
  display_name: "Microfeed"
  short_description: "Repo-specific patterns and workflows for microfeed"
  default_prompt: "Use the microfeed repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true